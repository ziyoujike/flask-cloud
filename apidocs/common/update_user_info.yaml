修改用户信息
---
tags:
  - 公共模块
parameters:
  - name: password
    in: body
    type: string
    required: true
    description: 手机号
    schema:
      #值类型
      type: object
      #定义属性
      properties:
        password:
          type: string
          description: 密码
        email:
          type: string
          description: 邮箱
        avatar_url:
          type: string
          description: 头像
        user_name:
          type: string
          description: 用户名


responses:
  schema:
    #值类型
    type: object

